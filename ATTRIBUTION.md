# Attribution

The vocabulary, example sentences, exercises and lesson structure in
`content/` were extracted from German course notes
(`enciclopedia/docs/german/`, A1→B2), which carry an MIT license. The
upstream files are never modified; corrections live in
`content/overrides/` so every fix is visible and re-extraction stays
byte-stable.
